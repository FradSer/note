scopes:
    - name: src
      description: Core application logic and business rules in Sources/; does not cover testing or skill-specific logic.
    - name: skills
      description: Domain-specific extensions and references in skills/; does not cover core application logic.
    - name: worker
      description: Background sync API and database migration logic in worker/; does not cover the main Swift application.
